{
  "buildCommand": "cd crates/web && trunk build --release",
  "outputDirectory": "crates/web/dist",
  "installCommand": "rustup target add wasm32-unknown-unknown && cargo install trunk",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
